fun isPalindrome(str: String): Boolean {
    val cleanedStr = str.replace("[^a-zA-Z0-9]".toRegex(), "").toLowerCase()
    return cleanedStr == cleanedStr.reversed()
}

fun main() {
    val input = "A man, a plan, a canal: Panama"
    println(isPal
